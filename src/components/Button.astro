---
interface Props{
  id: string
  title: string
  classAlt?: string
}

const { id, title, classAlt } = Astro.props
---
<button id={id} type="button" title={title} class={`bg-white/70 border rounded-lg p-2 group border-white/50 ${classAlt}`}>
  <slot />
</button>